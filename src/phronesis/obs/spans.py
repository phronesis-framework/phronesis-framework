"""Span creation primitives for Phronesis components.

Exposes the ``@traced`` decorator and the ``start_span`` /
``start_span_async`` context managers as the recommended way of
producing spans from the framework code.
"""

from __future__ import annotations
